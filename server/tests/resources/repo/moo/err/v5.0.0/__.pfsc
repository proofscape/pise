# Change log contains a libpath that looks like the author forgot
# it's supposed to be relative to the repo, not absolute.
# This shouldn't raise an exception, but should cause a warning to be printed.

changeLog = {
    moved: {
        "test.moo.err.foo": null
    }
}
